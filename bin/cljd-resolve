#!/bin/sh
# The resolve daemon, speaking newline-delimited JSON-RPC on stdio.
# Safe to run from any directory -- the extension spawns this.
here=$(cd "$(dirname "$0")" && pwd)
exec bb --config "$here/../bb.edn" -m cljd-resolve.daemon "$@"
